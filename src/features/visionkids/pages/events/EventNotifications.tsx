// Event reminders/updates reuse the same shared notifications system built
// in Phase 7 (Social & Parents Hub) — no separate events-only notification
// table or UI, just this route for discoverability from the Events section.
export { default } from "@/features/visionkids/pages/social/Notifications";
