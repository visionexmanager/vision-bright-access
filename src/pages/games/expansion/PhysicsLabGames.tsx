import{useState}from"react";import{Button}from"@/components/ui/button";import{Slider}from"@/components/ui/slider";import{useGameEconomy}from"@/components/game/GameEconomyGate";import{gameManager}from"@/features/arcade/core/gameManager";import{balancedLever,magneticForce,pendulumPeriod,physicsScore,projectileRange,torque}from"@/lib/games/arcadePhysicsLabEngine";
const panel="mx-auto max-w-3xl space-y-6 p-4 sm:p-8";function Control({label,value,set,min=1,max=10}:{label:string;value:number;set:(n:number)=>void;min?:number;max?:number}){return <label className="space-y-2"><span className="flex justify-between font-bold"><span>{label}</span><output>{value}</output></span><Slider value={[value]} onValueChange={v=>set(v[0])} min={min} max={max} step={1}/></label>}
export function BalanceLab(){const[leftMass,setLeftMass]=useState(4),[leftDistance,setLeftDistance]=useState(3),[rightMass,setRightMass]=useState(3),[rightDistance,setRightDistance]=useState(3),[round,setRound]=useState(1),[score,setScore]=useState(0),[message,setMessage]=useState("Match the torque on both sides");const{settleGameResult}=useGameEconomy();const check=()=>{const left=torque(leftMass,leftDistance),right=torque(rightMass,rightDistance),correct=balancedLever(leftMass,leftDistance,rightMass,rightDistance),next=score+(correct?100:physicsScore(Math.abs(left-right)));setScore(next);if(round===4){setMessage(`Lab complete: ${next} points`);gameManager.recordScore(next);void settleGameResult(next>=320?"win":"loss","Balance Lab")}else{setRound(round+1);setMessage(correct?"Balanced":"Torque difference: "+Math.abs(left-right))}};return <section className={panel}><header className="flex justify-between"><strong>Trial {round} / 4</strong><span>{score} points</span></header><div className="grid gap-6 sm:grid-cols-2"><Control label="Left mass" value={leftMass} set={setLeftMass}/><Control label="Left distance" value={leftDistance} set={setLeftDistance}/><Control label="Right mass" value={rightMass} set={setRightMass}/><Control label="Right distance" value={rightDistance} set={setRightDistance}/></div><div className="grid grid-cols-2 rounded-xl bg-slate-950 p-8 text-center text-2xl font-black text-white"><span>{torque(leftMass,leftDistance)} Nm</span><span>{torque(rightMass,rightDistance)} Nm</span></div><Button onClick={check} className="w-full">Test balance</Button><p role="status" aria-live="polite">{message}</p></section>}
export function PendulumPuzzle(){const[length,setLength]=useState(1),[estimate,setEstimate]=useState(2),[round,setRound]=useState(1),[score,setScore]=useState(0),[message,setMessage]=useState("Estimate the pendulum period");const{settleGameResult}=useGameEconomy();const check=()=>{const actual=pendulumPeriod(length),points=physicsScore(Math.abs(actual-estimate)*5),next=score+points;setScore(next);if(round===4){setMessage(`Lab complete: ${next} points`);gameManager.recordScore(next);void settleGameResult(next>=300?"win":"loss","Pendulum Puzzle")}else{setMessage(`Measured ${actual.toFixed(2)} seconds`);setRound(round+1);setLength(round+1)}};return <section className={panel}><header className="flex justify-between"><strong>Trial {round} / 4</strong><span>{score} points</span></header><div className="grid min-h-56 place-items-center rounded-3xl bg-slate-950 text-white"><div className="text-center"><span className="block h-32 w-1 bg-cyan-300 mx-auto"/><span className="block h-12 w-12 rounded-full bg-violet-500"/></div></div><Control label="Pendulum length (m)" value={length} set={setLength} min={1} max={4}/><Control label="Estimated period (seconds)" value={estimate} set={setEstimate} min={1} max={5}/><Button onClick={check} className="w-full">Measure period</Button><p role="status" aria-live="polite">{message}</p></section>}
export function TrajectoryMaster(){const[speed,setSpeed]=useState(20),[angle,setAngle]=useState(35),[target,setTarget]=useState(38),[round,setRound]=useState(1),[score,setScore]=useState(0),[message,setMessage]=useState("Reach the target distance");const{settleGameResult}=useGameEconomy();const launch=()=>{const range=projectileRange(speed,angle),points=physicsScore(Math.abs(range-target)/2),next=score+points;setScore(next);if(round===5){setMessage(`Mission complete: ${next} points`);gameManager.recordScore(next);void settleGameResult(next>=350?"win":"loss","Trajectory Master")}else{setMessage(`Projectile travelled ${range.toFixed(1)} meters`);setRound(round+1);setTarget(target+4)}};return <section className={panel}><header className="flex justify-between"><strong>Launch {round} / 5</strong><span>Target {target} m · {score} points</span></header><div className="h-48 rounded-3xl bg-gradient-to-b from-cyan-200 to-emerald-200"/><Control label="Speed (m/s)" value={speed} set={setSpeed} min={10} max={30}/><Control label="Angle (degrees)" value={angle} set={setAngle} min={10} max={80}/><Button onClick={launch} className="w-full">Launch</Button><p role="status" aria-live="polite">{message}</p></section>}
export function MagnetLab(){const[a,setA]=useState(4),[b,setB]=useState(4),[distance,setDistance]=useState(2),[aligned,setAligned]=useState(true),[round,setRound]=useState(1),[score,setScore]=useState(0),[message,setMessage]=useState("Predict attraction or repulsion");const{settleGameResult}=useGameEconomy();const force=magneticForce(a,b,distance,aligned);const predict=(attract:boolean)=>{const correct=(force<0)===attract,next=score+(correct?100:0);setScore(next);if(round===5){setMessage(`Lab complete: ${next} points`);gameManager.recordScore(next);void settleGameResult(next>=400?"win":"loss","Magnet Lab")}else{setMessage(correct?`Correct. Force magnitude ${Math.abs(force).toFixed(1)}`:"Incorrect polarity prediction");setRound(round+1);setAligned(!aligned);setDistance(Math.min(5,distance+1))}};return <section className={panel}><header className="flex justify-between"><strong>Trial {round} / 5</strong><span>{score} points</span></header><div className="flex justify-center gap-10 rounded-3xl bg-slate-950 p-16"><span className="rounded bg-rose-500 px-10 py-8 text-2xl font-black text-white">N</span><span className={`rounded px-10 py-8 text-2xl font-black text-white ${aligned?"bg-rose-500":"bg-blue-500"}`}>{aligned?"N":"S"}</span></div><Control label="Magnet A strength" value={a} set={setA}/><Control label="Magnet B strength" value={b} set={setB}/><Control label="Distance" value={distance} set={setDistance} min={1} max={5}/><div className="grid grid-cols-2 gap-3"><Button onClick={()=>predict(true)}>Attract</Button><Button onClick={()=>predict(false)}>Repel</Button></div><p role="status" aria-live="polite">{message}</p></section>}
