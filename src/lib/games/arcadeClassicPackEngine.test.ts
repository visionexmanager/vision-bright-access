import{describe,expect,it}from"vitest";import{emptyBlocks,placeBlock,popBubbles}from"./arcadeClassicPackEngine";describe("Arcade classic pack engine",()=>{it("clears completed block rows",()=>{const g=emptyBlocks(2,4);g[1]=[1,1,0,0];expect(placeBlock(g,2).cleared).toBe(1)});it("pops connected groups of at least three",()=>expect(popBubbles([[1,1,0],[1,2,2],[0,2,0]],0,0).popped).toBe(3))});
