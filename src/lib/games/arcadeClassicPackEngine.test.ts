import{describe,expect,it}from"vitest";import{createBricks,emptyBlocks,hitBrick,moveSnake,placeBlock,popBubbles,stepSnake}from"./arcadeClassicPackEngine";describe("Arcade classic pack engine",()=>{it("moves and grows snake on food",()=>expect(stepSnake([{x:2,y:2},{x:1,y:2}],"right",{x:3,y:2})).toMatchObject({ate:true,hit:false,snake:[{x:3,y:2},{x:2,y:2},{x:1,y:2}]}));it("detects snake walls",()=>expect(moveSnake([{x:0,y:0}],"left").hit).toBe(true));it("clears completed block rows",()=>{const g=emptyBlocks(2,4);g[1]=[1,1,0,0];expect(placeBlock(g,2).cleared).toBe(1)});it("removes the nearest live brick in a column",()=>expect(hitBrick(createBricks(2,2),0).filter(b=>b.alive)).toHaveLength(3));it("pops connected groups of at least three",()=>expect(popBubbles([[1,1,0],[1,2,2],[0,2,0]],0,0).popped).toBe(3))});
