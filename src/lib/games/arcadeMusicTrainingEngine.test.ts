import{describe,expect,it}from"vitest";import{matchesMelody,nextMelody,noteFrequency,pianoLessonScore,rhythmAccuracy}from"./arcadeMusicTrainingEngine";
describe("Arcade music training engine",()=>{it("maps maintained notes to concert frequencies",()=>{expect(noteFrequency("A4")).toBe(440);expect(noteFrequency("C5")).toBeGreaterThan(noteFrequency("C4"))});it("checks melody prefixes",()=>{expect(matchesMelody(["C4","E4"],["C4"])).toEqual({correctPrefix:true,complete:false});expect(matchesMelody(["C4","E4"],["C4","E4"]).complete).toBe(true)});it("scores timing and piano accuracy",()=>{expect(rhythmAccuracy([0,500,1000],[0,500,1000])).toBe(100);expect(pianoLessonScore(["C4","D4"],["C4","E4"])).toBe(50)});it("creates bounded deterministic lessons",()=>{expect(nextMelody(1)).toHaveLength(3);expect(nextMelody(10)).toHaveLength(6)})});
