import{describe,expect,it}from"vitest";import{balancedLever,magneticForce,pendulumPeriod,physicsScore,projectileRange,torque}from"./arcadePhysicsLabEngine";describe("Arcade physics lab engine",()=>{it("calculates torque and balanced levers",()=>{expect(torque(4,3)).toBe(12);expect(balancedLever(4,3,6,2)).toBe(true)});it("calculates a one-meter pendulum period",()=>expect(pendulumPeriod(1)).toBeCloseTo(2.006,2));it("maximizes ideal projectile range near 45 degrees",()=>expect(projectileRange(20,45)).toBeGreaterThan(projectileRange(20,25)));it("distinguishes magnetic attraction and repulsion",()=>{expect(magneticForce(4,4,2,true)).toBe(-4);expect(magneticForce(4,4,2,false)).toBe(4)});it("converts measurement error into bounded score",()=>{expect(physicsScore(0)).toBe(100);expect(physicsScore(20)).toBe(0)})});
