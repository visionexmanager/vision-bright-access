export type BlockGrid=number[][];export function emptyBlocks(rows=12,columns=8):BlockGrid{return Array.from({length:rows},()=>Array(columns).fill(0))}
export function placeBlock(grid:BlockGrid,column:number,width=2){const next=grid.map(r=>[...r]);let row=next.length-1;while(row>=0&&Array.from({length:width},(_,i)=>next[row][column+i]).some(Boolean))row--;if(row<0||column+width>next[0].length)return{grid,placed:false,cleared:0};for(let i=0;i<width;i++)next[row][column+i]=1;const remaining=next.filter(r=>!r.every(Boolean)),cleared=next.length-remaining.length;return{grid:[...Array.from({length:cleared},()=>Array(next[0].length).fill(0)),...remaining],placed:true,cleared}}
export type Bubble=0|1|2|3;export type BubbleGrid=Bubble[][];export function popBubbles(grid:BubbleGrid,row:number,column:number){const color=grid[row]?.[column];if(!color)return{grid,popped:0};const seen=new Set<string>(),queue:[[number,number]]|[number,number][]=[[row,column]],group:[number,number][]=[];while(queue.length){const[r,c]=queue.shift()!,key=`${r}:${c}`;if(seen.has(key)||grid[r]?.[c]!==color)continue;seen.add(key);group.push([r,c]);[[1,0],[-1,0],[0,1],[0,-1]].forEach(([dr,dc])=>queue.push([r+dr,c+dc]))}if(group.length<3)return{grid,popped:0};const next=grid.map(r=>[...r]);group.forEach(([r,c])=>next[r][c]=0);return{grid:next,popped:group.length}}
