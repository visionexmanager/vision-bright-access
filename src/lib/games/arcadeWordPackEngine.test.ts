import{describe,expect,it}from"vitest";import{CROSSWORD_CLUES,checkCrossword,isAnagram,normalizeWord,validWordStep}from"./arcadeWordPackEngine";describe("Arcade word pack engine",()=>{it("normalizes safe letter input",()=>expect(normalizeWord(" Key-board! ")).toBe("KEYBOARD"));it("checks crossword answers",()=>expect(checkCrossword(CROSSWORD_CLUES[0],"keyboard")).toBe(true));it("recognizes non-identical anagrams",()=>{expect(isAnagram("LISTEN","silent")).toBe(true);expect(isAnagram("LISTEN","listen")).toBe(false)});it("allows exactly one changed ladder letter",()=>{expect(validWordStep("COLD","CORD")).toBe(true);expect(validWordStep("COLD","WARM")).toBe(false)})});
