import{describe,expect,it}from"vitest";import{ADVANCED_QUIZ_BANKS,validateQuizBank}from"./advancedQuizBanks";describe("Advanced quiz banks",()=>{it("contains four distinct maintained topics",()=>expect(Object.keys(ADVANCED_QUIZ_BANKS)).toHaveLength(4));it("gives every question four unique answers and a valid key",()=>Object.values(ADVANCED_QUIZ_BANKS).forEach(bank=>expect(validateQuizBank(bank)).toBe(true)));it("does not duplicate questions across topics",()=>{const questions=Object.values(ADVANCED_QUIZ_BANKS).flatMap(bank=>bank.questions.map(item=>item.question));expect(new Set(questions).size).toBe(questions.length)})});
