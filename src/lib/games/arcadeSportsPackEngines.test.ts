import{describe,expect,it}from"vitest";import{archeryScore,bowlingPins,dartScore,golfStrokes,targetDistance}from"./arcadeSportsPackEngines";
describe("Arcade sports pack engines",()=>{it("scores a perfect calibrated target",()=>expect(targetDistance(72,50,72)).toBe(0));it("converts golf distance into strokes",()=>{expect(golfStrokes(4)).toBe(1);expect(golfStrokes(40)).toBe(4)});it("awards a bowling strike at optimal release",()=>expect(bowlingPins(78,50)).toBe(10));it("scores archery rings",()=>expect(archeryScore(72,50)).toBe(10));it("scores the dart bullseye",()=>expect(dartScore(50,50)).toBe(50))});
